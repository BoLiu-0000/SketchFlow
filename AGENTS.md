# Repository workflow

- Track product documentation and source code in the root Git repository.
- Do not commit dependencies, generated build output, local environment files, caches, or temporary archives.
- After completing and validating each requested code or documentation change, create a Git commit that describes that change.

